# Validation Application Settings

# Monitor endpoints (if applicable)
# Note: Current implementation uses simulated validation
# In production, monitors would expose HTTP health endpoints
DOTNET_MONITOR_URL=http://localhost:8001
JS_MONITOR_URL=http://localhost:8002

# Feature flag being monitored
FEATBIT_FLAG_KEY=test-flag

# Validation check interval in seconds
VALIDATION_INTERVAL_SECONDS=30

# Time window to detect configuration changes (seconds)
CONFIG_CHANGE_WINDOW_SECONDS=15
